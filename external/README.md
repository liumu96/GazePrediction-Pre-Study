# External Checkouts

Use this directory for local upstream repositories that support the sandbox.

Current local checkout:

- `projectaria_tools/`: Project Aria tools from
  `https://github.com/facebookresearch/projectaria_tools.git`.

This directory is ignored by the parent repository so upstream code is not
vendored into the ADT sandbox history.
